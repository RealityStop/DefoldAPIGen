<UserControl x:Class="APIEditor.Views.GenerationControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:APIEditor.Views"
             xmlns:viewModels="clr-namespace:APIEditor.ViewModels"
             d:DataContext="{d:DesignInstance viewModels:GenerateVM}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="4,4">
        <Button Content="Open Log" Command="{Binding OpenLogCMD}"/>    
        <Button Content="Generate" Style="{StaticResource ButtonSuccess}" Command="{Binding GenerateCMD}"/>    
    </DockPanel>
</UserControl>
